import {renderComponent, expect} from '../test_helper';
import CommentList from '../src/components/comment_list';

describe('comment_list', () => {
	it('shhould have list class', () => {
		expect(component.find('comment_list')).to.exist;
	})	
});
